<%= form_for(@pedido) do |f| %>
  <% if @pedido.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pedido.errors.count, "error") %> prohibited this pedido from being saved:</h2>

      <ul>
      <% @pedido.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :m2 %><br>
    <%= f.text_field :m2 %>
  </div>
  <div class="field">
    <%= f.label :orden %><br>
    <%= f.date_select :orden %>
  </div>
  <div class="field">
    <%= f.label :recibido %><br>
    <%= f.date_select :recibido %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
